# How to do a specific task

> [!NOTE]
> **Goal:** A one-paragraph recipe for accomplishing a single, well-scoped task in this project.
> **Prereqs:** You've completed [Getting started](01-Getting-Started) and have a working local environment.

This is a seed how-to. Replace the body with a real recipe when a real task earns its own page. How-tos answer "how do I X?" — they skip the narrative and go straight to the steps.

## Steps

1. _Identify the exact task in one sentence — a how-to does one thing._
2. _State each action as a concrete command or file change. Copy-pasteable is ideal._
3. _End on a verification step — a command or file state that confirms the task succeeded._

## Variants

_If the task has meaningful variants (different OS, different mode, different flag), give each one a sub-heading and a short block. Skip this section if there are no variants._

## Verify

_How to confirm the task actually succeeded — a command's output, a file's presence, a log line. One line is usually enough._

## Troubleshooting

| Symptom | Fix |
|---|---|
| _(populated as real issues surface)_ | _(fix guidance — link to [First reference](First-Reference) if the fix is a specific flag or command)_ |

See [First reference](First-Reference) for the canonical surface this recipe operates on.
