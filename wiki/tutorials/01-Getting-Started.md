# Tutorial 1 — Getting started

> [!NOTE]
> **Goal:** Go from a fresh clone of this project to a working local environment, with the tests green.
> **Time:** ~10 minutes (varies by project).
> **Prereqs:** A machine with this project's runtime installed (the `/setup` phase will tell you which).

This is a seed tutorial. The `/setup` phase populates the specific commands and entry points for this codebase; until then, the steps below are placeholders that describe the *shape* of a good tutorial — numbered, outcome-oriented, reproducible.

## Step 1 — Clone the repo

_Populated by `/setup` with the actual clone command and destination directory._

## Step 2 — Install dependencies

_Populated by `/setup` from the project's package manifest (e.g. `package.json`, `Cargo.toml`, `pyproject.toml`)._

## Step 3 — Run the project locally

_Populated by `/setup` with the dev-server or CLI entry point, the port or command that signals the project is up._

## Step 4 — Run the tests

_Populated by `/setup` with the test command and the expected "all green" output._

## What you learned

- How to get from a fresh clone to a running local environment.
- Where the day-to-day commands live (install, run, test).
- What a healthy "green" state looks like on your machine — the bar for any future change.

## Next

- **Do a specific task:** see [First how-to](First-How-To) as a worked recipe.
- **Look up a detail:** see [First reference](First-Reference) for commands, flags, and configuration.
- **Understand the why:** see [First explanation](First-Explanation) for intent and trade-offs.
